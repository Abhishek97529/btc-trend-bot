# Reports

The root of this directory contains evidence for the active BTC strategy suite.
`legacy-markets/` contains generated charts and tables from the separate Nifty
and US-index research tracks.

Runtime account state does not belong here; it lives inside each strategy's
`runtime/` directory.
